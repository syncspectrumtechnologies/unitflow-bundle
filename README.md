# UnitFlow Production Bundle

This bundle contains two separate backend services:

- `unitflow-core-api` — ERP runtime/data-plane
- `unitflow-platform-api` — SaaS control-plane

They are designed to run with separate databases and communicate over the internal platform contract documented in each service.
# unitflow-bundle
